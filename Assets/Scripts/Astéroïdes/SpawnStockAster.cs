using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnStockAster : MonoBehaviour
{
    /*
    public Minage minage;

    public GameObject asterOr;

    public GameObject Spawnpoint1;
    public GameObject Spawnpoint2;
    public GameObject Spawnpoint3;
    public GameObject Spawnpoint4;
    public GameObject Spawnpoint5;
    public GameObject Spawnpoint6;

    public GameObject Parent;




    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        SpawnAsterStock();
        
    }
    public void destroyallchildren()
    {
        for (int i = 0; i < Parent.transform.childCount; i++) Destroy(Parent.transform.GetChild(0).transform.gameObject);
    }
    void SpawnAsterStock()
    {
        if (minage._score > 0 && minage._score <= 25 && Parent.transform.childCount<1)
        {
           Instantiate(asterOr, Spawnpoint1.transform.position, Spawnpoint1.transform.rotation, Parent.transform);
          

        }
        if (minage._score > 25 && minage._score <= 50 && Parent.transform.childCount < 2)
        {

            Instantiate(asterOr, Spawnpoint2.transform.position, Spawnpoint2.transform.rotation, Parent.transform);
        }
        if (minage._score > 50 && Parent.transform.childCount < 3)
        {

            Instantiate(asterOr, Spawnpoint3.transform.position, Spawnpoint3.transform.rotation, Parent.transform);


        }
        if (minage._score > 75 && Parent.transform.childCount < 4)
        {


            Instantiate(asterOr, Spawnpoint4.transform.position, Spawnpoint4.transform.rotation, Parent.transform);
        }
        if (minage._score > 100 && Parent.transform.childCount < 5)
        {
            Instantiate(asterOr, Spawnpoint5.transform.position, Spawnpoint5.transform.rotation, Parent.transform);

        }
        if (minage._score > 125 && Parent.transform.childCount < 6)
        {

            Instantiate(asterOr, Spawnpoint6.transform.position, Spawnpoint6.transform.rotation, Parent.transform);
        }


    }
    */
}
